# Application Configuration
app:
  port: 3000
  nodeEnv: production
  apiBaseUrl: https://api.yourdomain.com

# Database Configuration
db:
  type: mysql
  host: your-db-host
  port: 3306
  username: your-username
  password: your-password
  database: nest_next_app
  synchronize: false

# Redis Configuration
redis:
  host: your-redis-host
  port: 6379
  password: your-redis-password
  db: 0

# JWT Configuration
jwt:
  secret: your-production-jwt-secret-key
  refreshSecret: your-production-jwt-refresh-secret-key
  expiresIn: 15m
  refreshExpiresIn: 7d

# Throttle Configuration
throttle:
  ttl: 60000
  limit: 100
